//TODO: IMPORT ACTION TYPES

const initState = {
  loading: false,
  error: false,
};

export default (state = initState, action) => {
  switch (action.type) {
    case value:
      return { ...state };
    default:
      return state;
  }
}
